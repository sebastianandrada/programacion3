r -> read(cursor empieza al principio), 
w-> write (si no existe, lo crea), 
a-> all(el cursor empieza al final) */

copy(origen, destino)
unlink(archivo) -> borrar
echo fread($ar, filesize('archivo.txt'))